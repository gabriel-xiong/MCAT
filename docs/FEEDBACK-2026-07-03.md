# Reviewer feedback — Friday milestone (2026-07-03)

External reviewer feedback on the MVP demo / Friday deliverable, relayed by the builder. Recorded verbatim-faithful for the record; do not edit the "As received" section — add follow-ups under "Status / response".

**Verdict: PASS.** Next focus: tighten the shared-engine proof and MVP artifacts.

## As received

The demo was good for MVP. Positive on the direction of the MCAT dashboard with:

- the three separate scores,
- topic mastery,
- focus area, and
- readiness holding back when there is not enough data.

Also saw the mobile app running a review session in the emulator (helps the phone side).

The repo review showed good Rust work:

- the MCAT mastery query,
- protobuf service,
- Python access,
- Rust tests, and
- a Python-callable test.

The BrainLift lines up well with the product direction — memory, performance, readiness, and better error diagnosis.

**What to clean up next:**

1. Prove the mobile app is using the *same modified Rust engine*.
2. Clean install proof.
3. Undo and collection integrity proof.
4. Basic latency or reliability numbers.

> "Pass, but the next focus should be tightening the shared engine proof and MVP artifacts."

## Status / response (as of 2026-07-03)

Mapping the reviewer's next-focus items to current work (update as these land):

1. **Shared modified-Rust-engine proof (mobile).** Highest priority. — _addressed
   2026-07-03; verdict: **partial / honest gap found**._ The desktop fork
   **definitively** runs the modified engine (Rust tests + Python integration +
   live output), but the **shipped APK links the STOCK published rsdroid**
   (anki `25.09.2`, zero `anki.mcat` proto classes) — the mobile MCAT features are
   a faithful **Kotlin re-implementation**, not the modified Rust binary. Full
   build-graph analysis, on-device APK evidence, and the exact steps to compile
   the fork's `rslib` into the APK are in
   [`anki-android-MCAT/docs/SHARED-ENGINE-PROOF.md`](../../anki-android-MCAT/docs/SHARED-ENGINE-PROOF.md)
   (reproduce: `anki-android-MCAT/tools/verify_backend_provenance.sh`).
2. **Clean install proof.** — _mobile PROVEN (automated); desktop artifact
   verified, clean-VM run is a human step._ See
   [`CLEAN-INSTALL-PROOF.md`](CLEAN-INSTALL-PROOF.md) (mobile reproduce:
   `anki-android-MCAT/tools/clean_install_emulator.sh`;
   screenshot: `anki-android-MCAT/docs/artifacts/clean_install_launch.png`).
3. **Undo + collection integrity proof.** — _PROVEN (15/15 scripted checks)._
   See [`UNDO-INTEGRITY-PROOF.md`](UNDO-INTEGRITY-PROOF.md)
   (script: `anki-MCAT/tools/mcat_undo_integrity_check.py`; results:
   `docs/artifacts/undo-integrity-results.json`).
4. **Basic latency / reliability numbers.** — _MEASURED (small-n, dev hardware)._
   Mastery query ~1 ms median; perf round-trip ~2.3 ms; perf-sync ~26 ms. See
   [`LATENCY-RELIABILITY.md`](LATENCY-RELIABILITY.md)
   (script: `anki-MCAT/tools/mcat_latency_bench.py`; results:
   `docs/artifacts/latency-results.json`).

Overall theme: the scoring/AI/dashboard direction is validated; the gap is **MVP evidence artifacts** — provenance, install/undo/integrity proofs, and latency/reliability measurements.
